const commun = [1, 3, 4, 5];
const secret = [];

for (let i = 0; i < commun.length; i++ ) {
    secret.push(commun[i]);
}
console.log(secret);

const secretShiny = [1, 2, 3, 4, 5];
const divine = secretShiny.slice();
console.log("Abdulaib33 Dans Afs Au Commencement", divine);